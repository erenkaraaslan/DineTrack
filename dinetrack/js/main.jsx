import React from 'react';
import ReactDOM from 'react-dom';
import Index from './index';
import TipIndex from './tip';

// This method is only called once
ReactDOM.render(
  <div>
  	<TipIndex/>
  </div>,
  document.getElementById('reactEntry'),
);